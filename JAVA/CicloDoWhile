    //CICLO DO WHILE
		
		/*var cafe = 0;//Definimos la variable
		do{ System.out.println("cafe = " + cafe);//El ciclo se imprime por lo menos una vez antes de pasar por el bucle
			cafe++;
		}while( cafe < 3 );//Como la condicion es finita, el ciclo se corta en la tercera vuelta
