      //CICLO WHILE
      
		  var contador = 0; //Definimos la variable
      
	  	while ( contador < 3) { Mientras contador sea menor a 3 contador se incrementa en 1 en cada vuelta - Condicion de corte
			System.out.println("contador = " + contador);
			contador++; }
